version: '3'

services:
  web:
    build: .
    command: gunicorn ecommerce_senzil.wsgi:application --bind 0.0.0.0:8000
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - db.sqlite3:/app/db.sqlite3
